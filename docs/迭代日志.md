# 迭代日志

> 项目:inventory-system(企业级进销存,一期已交付)
> 约定:**每次有实质变更(代码/文档/数据库/配置),追加一条到本文件,与代码同 commit。**
> 格式:日期 | commit | 变更摘要 | 验证 | 遗留
> 状态标记:✅ 已完成验证 / ⏳ 进行中 / ⚠️ 已知问题 / 📋 待排期

---

## 当前总览(2026-09-12)

- **测试**:后端 109/109 全绿(15 个测试类),checkstyle 0 违规,前端 build 0 错
- **git**:main 分支,5+ 提交
- **运行**:后端 8081(IDEA)/ 前端 5173(IDEA)/ PG 5433(docker)
- **账号**:admin/admin123、zhangsan/zhang123、lisi/lisi123

### 未决项 / 待排期

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 菜单管理(动态菜单 CRUD) | 📋 二期 | 一期不做,归二期 RBAC(用户已确认) |
| 2 | 财务联动(应收/应付) | 📋 二期 | 用户已确认 |
| 3 | E2E 7 bug 中 ⑤"销售新建错链采购路由" | ✅ 已确认不存在 | 09-11 核查:SalesOrderNewPage 路由全部正确,疑似当时临时问题或已随 phase1-fix 修复 |
| 4 | 上线前改 admin 默认密码 | 📋 待办 | 部署时必做 |

---

## 迭代记录(新在上)

### 2026-09-12

| 时间 | commit | 变更 | 验证 |
|---|---|---|---|
| 11:05 | 待提交 | **单据新增/编辑界面统一重设计**(用户反馈"毫无设计感";4 页统一骨架,业务逻辑零改动):① 页面头部 返回箭头+标题(编辑态带单号)+ 单全宽卡片(表头/明细分区,竖线分区标题)+ 底部 sticky 操作条(左"N 行明细·金额以服务端价税重算为准",右 添加行/保存,入库/出库多取消);② 表头四列对齐(用户拍板"表头四列然后对齐"):采购/销售 ProForm vertical+grid+colProps span:6,入库/出库原生控件 Row/Col span:6 显式栅格,备注独占整行;③ 销售页老式 Card+Form 表头 Pro 化(明细保留手工 Table 不强行 EditableProTable);④ 坑:ProForm.Item 是 antd Form.Item 别名不认 width/colProps(grid 静默 no-op),ProForm grid 仅 vertical 生效;入库/出库因此用原生 Row/Col | build 0 错;浏览器 4 页实测:首行 left 采购/销售 244/488/733/977 严格一致、入库/出库 244/490/737 三列+备注整行;业务属性 diff 逐字成对核对(联动/校验/payload/回填零改动);入库选关联采购单 CG-0003→模板行生成(螺丝 M8 已绑行);采购新建/编辑闭环、入库提交链路此前已实测通过 |

### 2026-09-11

| 时间 | commit | 变更 | 验证 |
|---|---|---|---|
| 22:45 | `27bf558` | **列表行高统一 45px**(用户拍板"全都调整为45"):① theme.ts Table token cellPaddingBlock/MD/SM=11(11+22行高+1边框+11=45;坑:antd 5.x Table 默认 size="middle",middle 档 token 独立且 CSS 后加载,只改基础 token 静默 no-op);② 库存查询页物品列双行(名称+编码)改单行+ellipsis;③ 预警页物品列补 width+ellipsis(原换行撑 61px);④ 物品/仓库/库位/字典操作列 Button type=link size=small(24px)→a.action-edit(22px,与其余 9 页一致),补 .action-edit.danger 红色+图标垂直居中规则;⑤ 字典页 Space→flex div(Space item 包裹层基线对齐 +2px) | build 0 错;浏览器 16 页实测:15 页精确 45px;字典页 45.5px 亚像素舍入显示 46(CSS 值已精确 45,渲染引擎取整噪声,非布局差异) |
| 21:49 | `796daa8` | **单据草稿态可编辑**(采购/销售/调拨/调整,提交审核前可再编辑):① 调整单补 update(PUT /stock-adjusts/:id,守卫仅草稿/已驳回,行明细全量替换——create 的写行逻辑抽私有方法 insertLines 复用;已驳回编辑后回 draft 并清驳回原因,坑:updateById 默认忽略 null 字段,需 LambdaUpdateWrapper 显式 SET NULL;采购/销售/调拨已有 update 守卫未动);② 新增 StockAdjustServiceTest 7 例(草稿/已驳回编辑成功+行替换生效,待审批/已审批/已完成/已作废/不存在 id 全拒);③ 前端 4 列表页操作列加"编辑"(仅 draft/rejected 行):采购/销售 NewPage 加编辑模式(路由 /new/:id? 进页 GET 回填表头+行,行 key 1..n 不与新建自增 key 冲突),调拨/调整复用 Drawer 回填(批次/库位原样保留只回传不展示),adjustApi 补 update 封装,AdjustCreatePayload 类型补 scrap;④ 顺手清 HEAD 遗留 checkstyle 违规 4 处(DateRangeSupport 23:59:59 魔数提常量、StockQueryServiceImpl 无用 import) | 109/109 全绿(原 102+新增 7);checkstyle 0 违规;mvn package BUILD SUCCESS;web tsc+vite build 0 错 |
| 21:30 | `d3360af` | **列表页紧凑化收口**(用户反馈筛选区稀疏/行太高/按钮文案长/菜单高亮错):① 筛选区 span=8→6,不展开一行 4 列(17 页 ProTable search.span,QueryFilter 官方属性,上一轮 colSize 凑行方案作废);② 采购/销售三合一金额列拆为 金额/税额/价税合计 三独立列(修 69px 行高);③ 物品扩展属性列 ellipsis+nowrap 单行省略(修 69px 行高);④ 14 个列表页按钮 新建XXX→新建;⑤ 菜单选中 startsWith 前缀误匹配修复(/stock-adjusts 误中"库存查询",改 startsWith(path+"/") 边界匹配) | build 0 错自跑;浏览器逐页实测:采购首行 4×span6、客户/物品 3 字段+按钮同行、调拨 5 字段 4+1;采购/销售/物品行高 69→47/49px;14 按钮文案全"新建"(git diff 核对无误伤 Drawer 标题);6 页菜单高亮全对(含 /purchase-orders/new 子路由落"采购订单") |
| 21:10 | `1995113` | **新建采购订单页 Pro 化**(用户拍板"整体重新设计"+报 bug"首次进页选物品税率不带出"):Card+Form+antd Table → ProCard+ProForm(grid)+EditableProTable(value/onChange 受控)。根因:editable 控件挂载后外部 form.setFields 写入 store 但不重渲染已挂载控件(store 有值 UI 空,诊断时序坐实);修法:联动值写回 value/data,controlled 路径组件自动 setFieldsValue 同步。坑:colSize 是 span 倍数非 span 值(pi 误用 16→col-24 占满整行,验收抓回) | build 0 错自跑;浏览器全链路:刚进页立即选物品→税率 UI 即时 13.00(修复前空)、手动输入不受干扰、加 2 行提交 90.00/11.70/101.70 服务端价税重算正确 |
| 19:15 | `76e448c` | **日期查询参数统一**(全项目唯一入口 DateRangeSupport):① 单据 5 页(采购/入库/出库/销售/调拨)parseDate(yyyy-MM-dd,东八区);② 流水页 parseDateTimeStart/End(首选空格分隔 yyyy-MM-dd HH:mm:ss,兼容 T 分隔/纯日期/Z;纯日期起=00:00:00、止=23:59:59);③ StockQueryServiceImpl 删私有 parseDateTime 改走统一入口;④ 前端流水页 transform 由 T 分隔改空格分隔(统一约定);⑤ 新增 DateRangeSupportTest 9 例(空格/纯日期/T/Z/格式错全覆盖) | 102/102 全绿;build 0 错;重启实测流水接口:空格 09-11 全天 18→7、纯日期 09-11~09-11 →7(止含当天)、T 分隔→7、Z→7、格式错→400"开始时间 格式错误,应为 yyyy-MM-dd HH:mm:ss" |
| 18:22 | `76e448c` | **修复列表日期区间筛选 500**(ProTable 试点时挖出的既有 bug):采购/入库/出库/销售/调拨 5 个列表接口带 from/to 参数一律 500——Service 拿 String 直接和 date 列做 ge/le,PostgreSQL 无 date 与 varchar 隐式比较运算符,抛 operator does not exist。新建 `common/support/DateRangeSupport.parseDate`(解析为 LocalDate + 格式校验抛 BizException),5 个 Service 统一改用。E2E 未覆盖日期筛选故此前未暴露 | 93/93 全绿;后端重启实测:from+to=2026-09-10→3 条、09-01~09-11→9 条、to only→3、格式错(2026/09/10)→400 业务错误"日期起 格式错误,应为 yyyy-MM-dd"(非 500);psql 对照同条件查询=3 行 |
| 18:25 | `1f2da33` | **列表页迁移 ProTable 试点 2 页**(库存查询/采购订单,后续铺开):`@ant-design/pro-components@2.8.10`(peer antd ^5.11.2 满足);筛选字段由 columns 配置驱动,新建按钮经 search.optionRender 放筛选行右侧(签名 (searchConfig, props, dom),dom 是默认按钮数组);状态列 valueEnum 驱动筛选下拉+表格仍用 DocStatusTag 渲染;日期区间 valueType=dateRange + search.transform 映射 from/to;headerTitle={false} options={false} 保持无标题无工具栏风格 | build 0 错;浏览器实测:库存查询 3 筛选+行数正确、采购订单 状态=已关闭→6、详情 Modal 正常、新建按钮在筛选卡内、9 数据行(antd 固定列 measure-row 不计入) |
| 19:10 | `1f2da33` | **列表页迁移 ProTable 第二批(15 页,pi 执行+独立验收)**:inbound/outbound/sales/transfer/adjust/stocktake/item/warehouse/location/supplier/customer/user/transaction 13 标准页 + alert(Tabs 双 ProTable,各自 request+actionRef,刷新双表)+ dict(主表全量返回 pagination=false,关键字前端内存过滤,Drawer 内表不动);范式:筛选列 hideInTable/valueType、日期 toDay 双兼容 transform(pro-components 2.8.10 dateRange form 实收字符串数组,弹层选择才 dayjs)、新建按钮 optionRender、驳回/新建表单与筛选 form 解耦(独立组件/独立 form)。另修试点页 purchase 遗留的裸 value[0].format 隐患(字符串输入路径必炸);types 补 UserInfo.createdAt? | build 0 错自跑;浏览器 15 页逐页复测:采购日期区间 09-10~09-10→3/09-10~09-11→9、流水 09-11→7(与 DB 一致)、销售状态=草稿→1、预警双表筛选隔离+刷新双请求、字典"仓库"4→1、老页无回归;后端 0 新增改动(工作区既有 5 Service 为我先前的 500 修复) |
| 17:00 | `6fc8696` | **列表页操作按钮移回筛选卡右侧**(用户反馈独立工具条行"按钮又出去了":单独一行+左侧大片空白+与筛选卡有空隙):ListPageShell 删 page-toolbar 分支,extra 渲染进 filter-card 内右侧;global.css 删工具条规则,filter-card 改 flex(space-between + wrap),form flex:1。仅 2 文件,15 页组件零改动(接口不变)。窄屏实测按钮恒居右不挤压(form 收缩+控件纵堆优先) | 独立验收(npm build 0 错自跑);浏览器 4 页实测(仓库/库位/采购/物品):page-toolbar 0 残留、按钮在筛选卡内、与表单同行(Δtop<40px);"新建库位"点击弹 Modal 正常;vision 复核布局紧凑无失衡 |
| 17:28 | `6fc8696` | **预警中心+字典管理两页布局统一**(补齐最后 2 个不走标准骨架的列表页):AlertPage 删 PageHeader、Space 手写筛选改标准 Form(临期=仓库下拉、低库存=关键字,复用后端已有参数)、刷新按钮移入筛选行右侧、tableProps 改可选(ListPageShell,Tabs 数据区走 children);DictPage 删内联样式横幅+外层重复 padding、"新建类型"进筛选行右侧、主表加关键字前端内存筛选(字典配置表不加后端)、主表/Drawer 表包 table-card | 独立验收:build 0 错自跑;浏览器实测(注意 antd Tabs 不销毁旧面板,须按激活面板计数):/alerts 临期 2 行→低库存空态 0 行、切 tab 筛选表单自动清空无串参;"/dicts 无横幅、关键字"仓库"4→1 条、重置恢复 4、新建类型 Modal 正常;老页面抽查 3 个(/warehouses /purchase-orders /users)无回归;pi 临时造低库存数据实验后已查库核实还原(min_stock=NULL) |
| 17:10 | `fa3c771` | **全列表页筛选区统一(前后端,pi 执行+独立验收)** ① 后端:WarehouseQuery 补 keyword/warehouseType(原空类)、Inbound/OutboundDocQuery 补 docNo/status/from/to、ItemQuery 补 itemCategory,4 个 Service 的 list 加对应条件(StringUtils.hasText 防护 + trim);② 前端:仓库/库位/用户 3 页新建标准筛选区(采购页 filterNode 范式),客户/供应商 Input.Search 换标准表单,入库/出库/物品/流水 4 页补字段,调拨补日期 RangePicker;库位页 extra 原"筛选仓库"Select 移入筛选区(去重);api/index.ts 参数类型同步 ③ 说明:入库/出库单状态机为单态 finished(后端入库即完成,无审批流),筛选状态项按实际枚举给"已完成" | 独立验收(不复用 pi 自报):mvn test 93/93 全绿 + checkstyle 0 + npm build 0 错(均自跑);浏览器逐页实测——仓库"原材料"→1 条 RAW01/重置 3、用户"zhang"→1 条、物品分类"原料"→1 条(antd Select 需 mousedown 触发,首次误判已复测澄清)、库位/客户/供应商/出库/调拨/流水/销售筛选区字段与日期控件全核;红线文件 0 改动 |
| 17:00 | `e854375` | **ListPageShell 去页面标题横幅**(用户指出横幅一行纯占空间):15 页删 title,extra 按钮改独立工具条行(page-toolbar 右对齐,无筛选区页面也有位置)——首版误把按钮塞进筛选卡导致仓库页(无筛选区)按钮消失,已修正 | npm build 0 错;浏览器实测采购/仓库/库位 3 类页面,page-header 0 残留,按钮均在,vision 复核无错位 |
| 16:45 | `2ff5801` | **AGENTS.md 硬约束新增第 8 条**(用户要求):git 不默认提交——改完代码只报告"待提交",必须等用户明确说"提交"才 commit;顺带测试数 87→93 同步实测值 | 文档变更,无代码影响 |
| 16:20 | `440bffc` | **Mapper XML 去冗余**(用户指出):① 同名 AS 别名 15 处删除(`b.item_id AS item_id` 类纯冗余,列名本就如此);保留两类必要别名——`i.id AS item_id`(真改名,VO 属性 itemId)、表达式/聚合驼峰别名(`"daysLeft"`/`"availableQty"`/`"totalQty"`,PG 引号保驼峰,MyBatis 靠它映射);② 显式 ASC 4 处删除(PG 默认升序,写了纯噪音),DESC 非默认保留;StockMapper `batch_id AS batch_id` 删除后 HashMap 键仍经下划线转驼峰读 `batchId`,行为不变 | 93/93 全绿、checkstyle 0;重启实测:临期预警 2 行全字段映射正确(daysLeft=9 等)、低库存 0 行正常 |
| 15:50 | `03c1119` | **snake_case 引号残留清扫**:排查调拨单 #3 审批 400(业务校验"目的仓启用库位必填目的库位",遗留旧单补库位 H-01 后走 驳回→编辑→提交→审批 全链路 completed)时发现 snake_case 改造漏网的字符串内带引号列名——① 5 个 Service 8 处 `extra.put("\"approver\"")` 去引号;② `DocStateSupport` 两个方法 7 处 `wrapper.set/eq/in("\"status\"/\"updater\"/\"id\"")` 去引号,Javadoc 同步;③ `DictReferenceRegistry` 字典引用检查拼接 SQL 2 处去引号。小写引号名在 PG 下恰好仍命中(不炸)但属规范残留,全清。全仓 grep `put("\"` 0 残留 | mvn clean test 93/93 全绿、checkstyle 0;重启后端;单 #3 驳回→编辑(toLocationId=19)→提交→审批全 200,终态 completed,库存账实核对一致(目的仓新批次行 10,源仓 FEFO 扣 10) |
| 15:40 | `f09a8f5` | **Lombok 补漏**:① 4 个 DO 的 6 个全限定名字段(`java.time.LocalDate docDate`/`java.math.BigDecimal minStock` 等)改正常 import,连带删 6 个残留手写 getter(全限定返回类型导致首轮批量删除漏网);② PageQuery(分页基类)4 个 getter/setter 换 `@Getter @Setter`,Spring MVC 参数绑定不受影响;③ JwtProperties 纯 POJO 同样简化。全仓扫描确认剩余 get/is 方法均为业务方法(isTerminal/isDictReferenced/getRefDisplayName 等),非样板 | 93/93 全绿、checkstyle 0;IDEA 重启登录 200 |
| 15:35 | `ab1d78e` | **首页 6 张 KPI 卡同行等高**:`.stat-card` 加 `height:100%` + `justify-content:center`,两张预警卡外层 Link 加 `display:block; height:100%`(否则 Link 断掉高度传递) | 浏览器 JS 实测 6 卡全部 110px + vision 复核无错位 |
| 15:00 | `51cb623` | **表/列名统一小写蛇形 + 引入 Lombok** ① V6 迁移:两库 26 表 + 203 列 RENAME 蛇形(`User` 因 PG 保留字改 `sys_user`),驼峰残留 0;`map-underscore-to-camel-case` false→true 根治映射;全项目手写 SQL(XML/@Update/Wrapper.set/extra.put/测试)去引号 ② 审计四件套按用户要求纯代码化:65 个 DO 的 `@TableField(value="updated_at",fill=...)` 简化为 `@TableField(fill=...)`(fill 注解是 MP 填充钩子触发器不可删),handler 统一按属性名填充 ③ Lombok:43 个 class 删 800+ 行手写 getter/setter,`@Getter @Setter`(不用 @Data 防 toString 泄露密码哈希),record/StockOpResult 不动 ④ 修正坑:字典类型(warehouseType 等)是 dict 表**业务数据**且为 registry Map 键,不能蛇形化;statusCode 是前端契约 JSON 键不动;DictReferenceRegistry 三层=Map键camel/表snake/列snake | mvn clean test 93/93 全绿、checkstyle 0;IDEA 标准流程重启(新 PID 38136),浏览器实测:登录/仓库/物品/流水/字典查询 200、404 路由标准错误体、创建客户审计四字段自动填充正确,测试数据已清 |
| 13:30 | `fe69c71` | **修 404 处理 500 崩溃**:`GlobalExceptionHandler.handleNotFound` 一个 @ExceptionHandler 同时声明 NoResourceFoundException+NoHandlerFoundException,方法却把两个异常都当参数——Spring 只注入实际抛出的那个,另一参数无 resolver → 任何不存在路由都 500(IllegalStateException)。拆成 handleNoResource/handleNoHandler 两个方法共用私有 notFoundBody | 93/93 绿、checkstyle 0;IDEA 重启后实测 GET /api/v1/not-exist → 404 标准错误体「路由不存在: GET ...」✓、登录 200 ✓ |
| 13:15 | `fe69c71` | **model 包重组**:entity/dto/vo/query 四包移入 `model/` 下(数据层归拢,124 个类),全项目 365 处 import + XML resultType 改 `com.company.inventory.model.*`,package 声明同步;AGENTS.md/README 分层描述同步 | mvn clean test 93/93 全绿、checkstyle 0 违规;后端重启后登录/冒烟通过。**过程记录**:首版漏改 package 声明行(只改了带点的 import 引用)致"重复类"编译失败,IDEA 侧索引未同步曾报"build failed";修正 124 个 package 声明 + IDEA 重新加载后恢复 |
| 12:55 | (本批) | **前端质感升级 A 档**(洗默认模板感):① Design Token——theme.ts 主色 #1d4ed8→#3056d3、圆角 6→10、global.css 新增阴影三级令牌(--shadow-sm/md/lg)、主按钮/Logo 主色渐变、菜单选中态圆角竖条;② 首页 DashboardPage——页头欢迎语(按时段问候+预警数+数据截至时间)、6 KPI 卡加 40x40 语义色图标容器+大数字 tabular-nums+hover 上浮、预警卡红/橙左边框+浅底(0 时恢复普通)、Top10 数量列内嵌横向数据条;③ 列表页通用——采购/销售/调拨/盘点/调整 5 页操作列语义色(作废红/驳回橙/关闭灰/审批提交蓝)、金额列右对齐+tabular-nums;④ 顶栏——角色 Tag 红色描边改蓝色浅底、头像渐变底 | 独立验收:npm build 0 错复跑 ✓;浏览器实测——欢迎语/图标容器/预警卡红强调/数据条/操作列四色语义均渲染 ✓;vision 逐像素复核确认。**独立验收另修 2 缺陷**:① Top10 表 scroll x:480 强制最小宽致"数量"列在窄容器下被截断(表头只剩"数"),去掉强制宽改自适应,复测表头完整 ✓;② 任务书要求的"文本列 ellipsis 防折行"代理只落了部分,全项目 12 个创建人/操作人列(11 页)统一补 ellipsis: true,复测 zhangsan/e2e-approver 全行 nowrap 无折行 ✓ |
| 12:07 | (本批) | **全表审计字段统一**:UserContext(ThreadLocal) + AuditMetaObjectHandler(MyBatis-Plus 自动填充) + 15 个 DO 补 creator/createdAt/updater/updatedAt 字段及 fill 注解;V5 迁移 SQL ALTER 两张表(RENAME COLUMN)+ ADD 11 张表(ADD COLUMN IF NOT EXISTS);Supplier/Customer 列名 createdBy→creator/updatedBy→updater(DO/VO/Service 链路同步);前端 Warehouse/Item/Supplier/Customer 列表加"创建人"列;types/phase1.ts + types/index.ts 同步类型;6 条自包含测试覆盖 insert/update/明细行/供应商/库位/viewer 身份。**独立验收另修 1 缺陷**:updateFill 原用 strictUpdateFill(只填 null 字段),update 前 selectById 已把 updatedAt 带回非 null 导致 updater/updatedAt 永不刷新,改 setFieldValByName 强制覆盖;itemUpdateAudit 断言同步收紧为 isAfter 严格变新(实测 strict 实现下该测试确实报红,证明能抓回归) | 93/93 全绿(原 87+6 新)、checkstyle 0 违规、前端 build 0 错;两库(生产+测试)information_schema 各 67 审计列、旧列名 0 残留;浏览器 fetch 实测:创建 creator=admin,更新后 updatedAt 12:21:53.474→12:21:53.754 变新 ✓ |
| 11:15 | 见下 | **仓库/库位支持编辑**:后端补 `PUT /warehouses/{id}` 和 `PUT /locations/{id}`,编码/所属仓库锁死(`WarehouseUpdateDTO` 故意不含 warehouseCode 字段),含 3 条防数据不一致校验(enableLocation 1→0 在有库位/库位库存时 400;enableSerial 1→0 在有序列号台账时 400;停用在有 quantity>0 或 preAllocatedQty>0 的库存时 400,enableExpiry 1→0 放行);前端 WarehouseListPage / LocationListPage 接操作列"编辑"(admin)+ Drawer/Modal 双态,编辑时编码/仓库禁用回填;新增 10 条自包含测试覆盖正反路径 + viewer/operator 403 | 87/87 全绿(原 77+10 新)、checkstyle 0 违规、build 0 错;浏览器自测通过,数据已还原 |
| 10:5x | `bdf29ea` | **AGENTS.md 加入"优先走 IDEA MCP 通道"约定**:编译/运行/git/DB/重构/调试优先 IDE 侧,IDEA 未开才退回命令行;踩坑备忘补"启停走 IDEA 运行配置,别命令行另起实例" | IDEA MCP 实测:ping 通(56 工具)、git 仓库已识别(main/clean)、3 运行配置在位 |
| 10:4x | `fb01c0e` | **AGENTS.md 移到根目录并通用化**:按 agent 初始化惯例命名(AGENTS.md,各代理原生读取),内容改为命令优先的通用风格;删除 docs/AGENT.md | 内容取自项目实测事实 |
| 10:2x | `4fff12a` | **新增 docs/迭代日志.md**:迭代流水账,确立"动代码同 commit 记一笔"约定;README 挂索引 | git 历史 5 commit 核对 |
| 10:05 | `eb6157e` | **物品页支持编辑**:操作列"编辑"(admin)、抽屉编辑态、编码禁用、扩展属性 KV 自动回填、接 `PUT /items/:id`(后端接口一期已有,前端此前未接) | build 0 错;浏览器实测编辑名称保存成功、列表刷新、数据已还原 |
| 10:01 | `195cf6f` | **字典动态化 + 传统列表布局**:`DictType` 表(V4 迁移)+ 类型 CRUD 接口 + `DictReferenceRegistry` 注册表(替代硬编码 switch);DictPage 从左右分栏改回**类型主表格 + Drawer 二级管理**(用户反馈分栏不好看,三版迭代:表格→卡片→列表+Drawer) | 77/77、checkstyle 0、build 0;浏览器实测:新建类型/建项/Badge 联动/停用拦截/viewer 403 全过 |
| 09:30 | (含 195cf6f 前) | 字典卡片版验收 + **dayjs 中文 locale** 修复(日历星期头英文 Su/Mo/Tu 问题):`main.tsx` 加 `dayjs.locale("zh-cn")` | 日历实测 一/二/三...全中文 |
| 09:06 | `975c034` | **流水业务列全量中文 Tag**:8 种 bizCode 全映射(入库/出库/调拨入出/调整入出/预占/释放预占)+ 6 组新配色 + 筛选下拉同源补全 | build 0;与后端 ErrorCode 8 个 code 逐条核对 |
| 08:52 | `d942e41` | **README 全面同步**一期真实状态(17 Controller/26 Mapper/70 测试/权限矩阵 14 行/API 全量)+ 文档实时同步约定 | 数字均实测 |
| 08:50 | `ed0ce54` | **git 仓库初始化**(main),296 文件 38889 行,.gitignore 补全(target/.tmp/.idea 白名单) | 工作区干净 |
| 07:5x | (未单独 commit,含 d942e41) | **后端启动横幅**:`InventoryApplication` 加 ApplicationRunner 打印应用/API/Swagger 地址(读实际 server.port) | compile + checkstyle 过;需 IDEA 重启后端生效 |
| 07:0x | (同上) | **审批自批规则**:`ApprovalGuard` 组件(admin 可自批、operator 禁自批),5 Service 8 处接入,防"提交-审批"死锁 | 66→70 测试绿,3 场景实测 |
| 06:xx | (同上) | **opencode 字典管理界面**(MiMo V2.5 Free):DictPage + 4 admin 写接口 + 引用校验 + 4 测试 | 70/70、@RequireRole 全带、浏览器实测通过 |
| 前一日 | (同上) | **pi phase1-fix 收尾**:时间体系东八区统一(yyyy-MM-dd HH:mm:ss)+ JVM 时区锁定 + 权限完善 + 5 前端 bug + 结算方式字典接前端 | 65/65 绿,浏览器验证 |
| 前一日 | (同上) | **E2E 9 链路**(Claude --effort low):登录/供应商/客户/采购全链/销售预占发货/超卖拒绝/调拨/盘点调整/预警三角色,35 截图 | 全过,DB 直查核对;产出 7 bug 清单 |

### 2026-09-10(一期主体,commit 前历史,由 ed0ce54 一次性纳入)

- pi 执行一期:105→217 文件、16→17 Controller、前端 10+ 新页面(采购/销售/调拨/盘点/调整/预警/供应商/客户)
- Claude Code Java 重写(234 轮)+ 独立验收修 3 bug(PG 大小写/MapperScan/checkstyle)
- 查询/分页统一规范(方案 1):8 Query 类 + PageQuery + `{rows,total,page,pageSize}`
- 字典 3 组种子(warehouseType/itemCategory/settleMethod)+ GET /dicts + useDict
- P0 修复:订单新建 onItemChange 漏 itemId
- checkstyle 中文 Javadoc 适配(period=字面量 `。`)

---

## 关键决策(快照,详见 README)

1. 列表接口:方案 1 全量统一(Query DTO 绑定 + 全量分页)
2. 审批流:内置轻量 + ApprovalGuard(admin 可自批/operator 禁自批)
3. 字典:配置类才进字典,**状态机/流水枚举不进**(前端静态映射,见 `StatusTag.tsx`)
4. 字典类型:typeCode 建后不可改;停用代替删除;引用校验走 `DictReferenceRegistry`
5. 时间:东八区 JVM 显式锁定 + Jackson `yyyy-MM-dd HH:mm:ss`
6. 文档:README + 本文件,与代码同 commit,数字写实测值

## 工具分工(本项目实践)

| 工具 | 用途 | 结果 |
|---|---|---|
| Claude Code | 大单从零建(Java 重写、E2E) | 好用但贵,大单首选 |
| pi(Qwen3.8-27B xhigh) | 收尾迭代/小改动 | 快且稳,65 条测试交付 |
| opencode(MiMo V2.5 Free) | 中等单(字典界面/动态化) | 免费,质量合格;MiMo 有隐性限额,长任务可能断,断点续会话可恢复 |
| Hermes(我) | 分发/验收/小修/文档 | 验收必须独立复跑+浏览器实测+查库 |

## 踩坑备忘(本项目相关)

- bash 下 `mvn` 有 classworlds bug,一律 `bash scripts/mvn.sh`
- PG 表/列 PascalCase 带引号,手写 SQL 必须双引号
- 杀后端要 netstat 找 java PID 强杀,杀 mvn 壳不够
- curl 直连 8081 带 Authorization 会被本地代理吃 → 401;验证走 python urllib 或浏览器 fetch
- 统计测试数用 `grep -E "@Test\b"`,别用 `@Test`(@TestInstance 会误计)
- React 受控输入框 JS 直接 `.value=` 不触发 state,要走 native setter + dispatch input 事件
